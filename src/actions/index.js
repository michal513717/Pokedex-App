export const SET_POKEMONS = (item) => {
  return {
    type: "SET_POKEMONS",
    payload: {
      item,
    },
  };
};

export const SET_IMAGES = (item) => {
  return {
    type: "SET_IMAGES",
    payload: {
      item,
    },
  };
};

export const SET_DETAILS_POKEMON = (item) => {
  return {
    type: "SET_DETAILS_POKEMON",
    payload: {
      item,
    },
  };
};
